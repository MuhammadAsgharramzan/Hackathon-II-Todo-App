# Production Environment Configuration - EXAMPLE
# For deployment with cloud database (Neon PostgreSQL)
#
# IMPORTANT: Copy this file to .env.production and update with your credentials
# cp .env.production.example .env.production

# Database Configuration (Neon PostgreSQL)
# Replace with your actual Neon database connection string
DATABASE_URL=postgresql://your_user:your_password@your-host.neon.tech/your_database?sslmode=require

# JWT Configuration
# Generate a new secret: openssl rand -hex 32
JWT_SECRET_KEY=your-secret-key-here-change-this
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Gemini API Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Application Configuration
PORT=8000
NODE_ENV=production
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Note: This configuration uses Neon cloud database instead of local Postgres
# To use this configuration:
# 1. Copy this file: cp .env.production.example .env.production
# 2. Update .env.production with your actual credentials
# 3. Deploy: docker-compose -f docker-compose.cloud.yml up -d
